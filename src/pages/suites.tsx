import Inner from '@/components/Inner';

export default function SuitesPage() {
  return (
    <Inner backgroundColor={"brown"}>
      <main className="text-white text-center py-24 bg-black">
      ABOOT
      </main>
    </Inner>
  );
}
